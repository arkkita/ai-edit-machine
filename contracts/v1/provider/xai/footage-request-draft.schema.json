{
  "$comment": "Provider baseline only; M1 adapter conformance must verify the live API.",
  "$defs": {
    "FootageRequirementDraft": {
      "additionalProperties": false,
      "properties": {
        "alternatives": {
          "items": {
            "maxLength": 500,
            "minLength": 1,
            "type": "string"
          },
          "maxItems": 20,
          "type": "array"
        },
        "characters_or_relationship": {
          "items": {
            "maxLength": 500,
            "minLength": 1,
            "type": "string"
          },
          "maxItems": 20,
          "type": "array"
        },
        "media_title": {
          "maxLength": 500,
          "minLength": 1,
          "type": "string"
        },
        "moment_description": {
          "maxLength": 2000,
          "minLength": 1,
          "type": "string"
        },
        "order": {
          "minimum": 0,
          "type": "integer"
        },
        "quote_claim_id": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ]
        },
        "quote_status": {
          "$ref": "#/$defs/QuoteStatus"
        },
        "quote_text": {
          "anyOf": [
            {
              "maxLength": 500,
              "type": "string"
            },
            {
              "type": "null"
            }
          ]
        },
        "rationale": {
          "maxLength": 2000,
          "minLength": 1,
          "type": "string"
        },
        "required": {
          "type": "boolean"
        },
        "season_episode_or_asset": {
          "maxLength": 500,
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "order",
        "required",
        "media_title",
        "season_episode_or_asset",
        "characters_or_relationship",
        "moment_description",
        "quote_status",
        "quote_text",
        "quote_claim_id",
        "rationale",
        "alternatives"
      ],
      "type": "object"
    },
    "QuoteStatus": {
      "enum": [
        "NONE",
        "VERIFIED",
        "PARAPHRASE",
        "UNVERIFIED_LEAD"
      ],
      "type": "string"
    }
  },
  "$id": "https://ai-edit-machine.local/contracts/v1/provider/xai/footage-request-draft.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Provider-authored request; entity and requirement IDs are server-owned.",
  "properties": {
    "requirements": {
      "items": {
        "$ref": "#/$defs/FootageRequirementDraft"
      },
      "maxItems": 30,
      "minItems": 1,
      "type": "array"
    },
    "schema_version": {
      "const": "1.0.0",
      "type": "string"
    },
    "search_terms": {
      "items": {
        "maxLength": 500,
        "minLength": 1,
        "type": "string"
      },
      "maxItems": 30,
      "minItems": 1,
      "type": "array"
    },
    "summary": {
      "maxLength": 2000,
      "minLength": 1,
      "type": "string"
    },
    "warnings": {
      "items": {
        "maxLength": 500,
        "minLength": 1,
        "type": "string"
      },
      "maxItems": 20,
      "type": "array"
    }
  },
  "required": [
    "schema_version",
    "summary",
    "requirements",
    "search_terms",
    "warnings"
  ],
  "type": "object"
}
